// Default values
ext {
    keyAlias = 'main'
    keyPassword = 'com.perqin.copyshare'
    storeFile = rootProject.file('debug.keystore.jks')
    storePassword = 'com.perqin.copyshare'
}

def propFile = rootProject.file("credentials.properties")
if (propFile.canRead()) {
    def prop = new Properties()
    prop.load(new FileInputStream(propFile))
    ext.keyAlias = keystoreProperties['keyAlias']
    ext.keyPassword = keystoreProperties['keyPassword']
    ext.storeFile = file(keystoreProperties['storeFile'])
    ext.storePassword = keystoreProperties['storePassword']
}
